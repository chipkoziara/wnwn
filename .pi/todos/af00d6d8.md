{
  "id": "af00d6d8",
  "title": "Support stripping time component from deadline/scheduled while preserving date",
  "tags": [
    "task-detail",
    "datepicker",
    "dates"
  ],
  "status": "open",
  "created_at": "2026-03-15T18:20:50.525Z"
}

Allow datetime fields to be converted back to date-only while preserving the date.

## Acceptance Criteria

- Users can convert datetime → date-only without clearing the date.
- Writer/display remain consistent with existing date-only formatting conventions.
- Add tests for transitions (datetime → date-only and save/load roundtrip).

## Related todos

- `TODO-a84f69a7` — clearing deadline/scheduled values is adjacent but distinct; this todo specifically covers converting datetime fields back to date-only.
- `TODO-c5bc1cdf` — manual datetime validation should align with any UX for removing time precision.

## Dedupe note

This item remains canonical for **datetime → date-only precision downgrade while preserving the date**.
