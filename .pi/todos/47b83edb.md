{
  "id": "47b83edb",
  "title": "Add boolean logic and comparator completeness to query DSL",
  "tags": [
    "query",
    "views",
    "dsl"
  ],
  "status": "open",
  "created_at": "2026-03-15T18:20:50.525Z"
}

Support richer boolean filtering and comparator parity in the query DSL.

## Acceptance Criteria

- Support explicit boolean expressions (AND/OR/NOT) with predictable precedence and grouping.
- Add comparator aliases/equivalents (including `>=` and `<=`) where currently missing.
- Add parser + matcher tests covering mixed boolean/comparator expressions.

## Related todos

- `TODO-72aa4965` — relative time-series macros build on and extend the query language, but are not duplicates.
- `TODO-f694b2e2` — fuzzy search complements DSL filtering rather than overlapping with boolean/comparator support.

## Dedupe note

This item remains canonical for **boolean logic and comparator/operator completeness** in the DSL.
